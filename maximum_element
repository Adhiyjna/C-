#include<iostream>
using namespace std;

int main()
{
int n,i,arr[10];

    int maxt=0;
    cin>>n;
    for(i=0;i<n;i++)
    {
        cin>>arr[i];
    }

    for(i=1;i<n;i++)
    {
        if(arr[i-1]<arr[i])
        {
            maxt=arr[i];
        }
    }
        cout<<maxt;
}
